# Root-level env for docker-compose interpolation
# Copy to `.env` (gitignored) and fill in values.

# Frontend build-time variables used by web/Dockerfile
VITE_API_URL=http://localhost:8000
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=